{% extends 'base.html.twig' %}

{% block title %}Coach{% endblock %}

{% block body %}
    <h1>Coach</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ coach.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coach') }}">Retour</a>

    <a href="{{ path('app_adherent', {'id': coach.id}) }}">Modifier</a>

    {{ include('coach/_delete_form.html.twig') }}
{% endblock %}
