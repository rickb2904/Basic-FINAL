{% extends 'baseAdherent.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ inscription.date ? inscription.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscription_index') }}">Retour à la liste</a>

    <a href="{{ path('app_inscription_edit', {'id': inscription.id}) }}">Modifier</a>

    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}
